ull semiprime_gen(vector<ull>& primes){
	//seed a random number generator

	//variables for storage
	ull semiprime;

	//generate primes


	semiprime = primes.at(0) * primes.at(1);

	return semiprime;
}
